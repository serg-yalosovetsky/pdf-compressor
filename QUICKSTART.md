# 🚀 Быстрый старт

## Установка и запуск за 3 шага

### 1️⃣ Установите Ghostscript

**macOS:**
```bash
brew install ghostscript
```

**Ubuntu/Debian:**
```bash
sudo apt-get install ghostscript
```

### 2️⃣ Установите Python зависимости

```bash
pip install -r requirements.txt
```

### 3️⃣ Запустите сервер

**Вариант 1 - Простой запуск:**
```bash
python main.py
```

**Вариант 2 - Автоматический скрипт:**
```bash
./start.sh
```

**Вариант 3 - С настройками:**
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## 🌐 Откройте в браузере

После запуска откройте: **http://localhost:8000**

## 💡 Как использовать

1. Перетащите PDF файл или нажмите для выбора
2. Выберите качество сжатия (низкое/среднее/высокое)
3. Нажмите "Сжать PDF"
4. Файл автоматически скачается

## ⚙️ Уровни качества

| Качество | DPI | Описание |
|----------|-----|----------|
| **Низкое** | 72 | Максимальное сжатие, для просмотра на экране |
| **Среднее** | 150 | Оптимальный баланс размера и качества |
| **Высокое** | 300 | Для печати, минимальная потеря качества |

## 🔒 Безопасность

✅ Все файлы обрабатываются локально  
✅ Никакой загрузки в облако  
✅ Автоматическое удаление временных файлов  
✅ Максимальный размер файла: 100 MB  

## ❓ Проблемы?

**Ghostscript не найден:**
```bash
# Проверьте установку
gs --version
```

**Порт занят:**
```bash
# Используйте другой порт
uvicorn main:app --port 8080
```

**Ошибка при сжатии:**
- Убедитесь, что файл - валидный PDF
- Проверьте размер файла (макс. 100 MB)

---

**Готово!** Теперь у вас есть локальный сервис для сжатия PDF 🎉

